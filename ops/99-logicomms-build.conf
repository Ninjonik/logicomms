# The host runs several services with filesystem watchers.  Tauri needs an
# inotify instance while it packages a Windows release.
fs.inotify.max_user_instances = 1024
fs.inotify.max_user_watches = 1048576
